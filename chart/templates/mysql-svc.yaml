apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mysql.service }}
spec:
  selector:
    app: {{ .Values.mysql.service }}
  ports:
    - protocol: TCP
      port: {{ .Values.mysql.port }}
      targetPort: {{ .Values.mysql.port }}
  type: NodePort
  